package com.seckill.ordersystem.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.seckill.ordersystem.entity.StockFlow;

public interface StockFlowMapper extends BaseMapper<StockFlow> {
    // Define any custom query methods here if needed
    // For example:
    // List<StockFlow> findByProductId(Long productId);
    // StockFlow findByOrderNo(String orderNo);
}
