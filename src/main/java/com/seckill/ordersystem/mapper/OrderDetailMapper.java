package com.seckill.ordersystem.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.seckill.ordersystem.entity.OrderDetail;

public interface OrderDetailMapper extends BaseMapper<OrderDetail> {
    // Define any custom query methods here if needed
    // For example:
    // List<OrderDetailMapper> findByOrderId(Long orderId);
    // OrderDetailMapper findByProductId(Long productId);
}
