package com.seckill.ordersystem.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.seckill.ordersystem.entity.AccessControl;

public interface AccessControlMapper extends BaseMapper<AccessControl> {
    // Define any custom query methods here if needed
    // For example:
    // List<AccessControl> findByUserId(Long userId);
    // AccessControl findByRole(String role);
}
